node_modules/
dist/
coverage/
.vite/
*.tsbuildinfo

__pycache__/
*.pyc
.venv/
venv/

# editor scratch
*.swp
*.swo
*~
.idea/
.vscode/

.DS_Store

*.zip


# build
dist/
.vite/

# `pnpm bench`'s output and its work tree: a patched copy of src/ and one
# self-contained HTML file, both rebuilt from source on demand. Rooted, because
# an unrooted `bench/` also swallows `tools/bench/`, which is the source.
/bench/


# Reference screenshots the author drops in for a note. Kept out of history:
# they are conversation, not source.
ex.png
IMG_*.jpg
screenshot.png
prompt*.md

# Worktrees the harness creates for isolated agents.
.claude/worktrees/
