# esbuild ships its native binary via an optional platform package, so its
# postinstall script is not needed. Denied rather than approved: no third-party
# install scripts run in this repo.
allowBuilds:
  esbuild: false
