index.html

# Author-owned material: not ours to restyle. Anything under docs/ belongs to
# the author unless it is a document this codebase generates and maintains.
docs/
!docs/PORT_NOTES.md
golden/
dist/
pnpm-lock.yaml

# Claude Code's own state, including git worktrees — those are full checkouts of
# this repo and are formatted on their own branch, not from this working tree.
.claude
