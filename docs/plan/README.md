# The plan

Aphelion is being rebuilt from two sources: `docs/VISION.md`, which says what the game is
for, and `docs/design/`, twelve design directions that say what it looks like when it is.
A working prototype exists at `~/git/aphelion` and is consulted, never copied (ADR-0001).

This directory is the implementation plan. It is written for sessions that have not read
the conversation that produced it.

## Before you touch anything

Read, in this order: `CONTEXT.md` (the glossary — the words in it are the words to use),
`docs/adr/` (fifteen decisions, all of them still binding), the spec for the step you are
doing, and the design board that spec cites.

## How a step works

Every step names its **spec**, its **acceptance criteria**, its **verification command**,
and whether it ends in an **author gate**.

- One branch per step, one PR into `main`, squash-merged. A step is sized to one session.
- A step is done when `pnpm check` is green, its acceptance criteria are demonstrably met,
  and its status is updated in the table below.
- **A gated step stops.** Do not start the next step, do not "get a head start", do not
  decide the gate is a formality. The author flies the build and says yes. Gates exist
  because determinism can prove the game is correct and cannot prove it feels good
  (ADR-0004).
- If a spec is wrong, silent or contradicts another spec, **stop and say so**. Do not
  invent the ruling. Every contradiction anyone found while writing this plan was resolved
  by the author and recorded; a new one deserves the same.

## The milestones

Each ends in something that runs on a phone. That is deliberate: the author's gate is
playing it, so a plan whose first playable moment is late is a plan that cannot be gated.

| # | Milestone | Ends with | Gate | Status |
|---|-----------|-----------|------|--------|
| [M0](./m0-foundations.md) | Foundations, specs, renderer verdict | Specs written, a field you can pan, a renderer ADR | — | **M0.1–M0.4 done** ([specs](../spec/), scaffold, the three layers and `pnpm portable`, the QR dev server — scanned, and the phone shows the tick counter); CI and the Pages deploy are written but have never run, since the repo has no remote yet and pushing it is deferred; **M0.5 done** — measured on the author's phone at the design size, Canvas2D holds at p99 3ms against an 8ms budget ([ADR-0011](../adr/0011-canvas2d-carries-the-design.md), reports in `diagnostics/`); the spike is deleted as planned |
| [M1](./m1-the-swing.md) | The swing | Crude shapes, real gravity, a flyable swing | **author** | **M1.1 done** — [spec 01](../spec/01-swing.md) is written, from 474s of recorded phone play under the prototype's current physics tuning plus headless sweeps of its own simulation. Every characteristic has a number, a source and a tolerance; pillar 2's tension is now a measurement (**43%** of release headings are reachable at peak boost, never above 49% at any sampled geometry) and §11's first criterion is M1.3's acceptance gate. The **hitstop is rejected** and the punch is bought with speed instead ([ADR-0012](../adr/0012-the-punch-is-bought-with-speed-not-with-stopped-time.md)); spec 02 carries a notice and M2.4 rebases its timeline. **M1.2 done** — the simulation core: fixed timestep with a bounded catch-up, bodies with mass derived from radius, gravity that acts **only from a held body**, a substepped integrator, a seeded stream and a byte-level snapshot. A recipe replayed twice is byte-identical at every one of 3600 ticks. **Six substeps is proved rather than inherited**: measured against a 96-substep reference on this integrator, six lands a factor of four inside spec 01 §12's tolerances and one substep does not. **Spec 01 §12a is closed** — the simulation owns `sin`, `cos` and `atan2`, because V8 and JavaScriptCore disagree on them for 4.3%, 4.6% and 17.9% of arguments and a recipe has to cross that pair ([ADR-0014](../adr/0014-the-simulation-owns-its-transcendentals.md)). **§13.3 is closed too**: the author confirmed the ×3 / ×27 conversion, carried as one named constant. **One ruling still open** in the spec README: the mass-to-radius exponent, deferred to the M1 gate and carried as a parameter with `n = 0` reproducing the prototype exactly. **M1.3 done** — the one verb, end to end: a press takes the body it is arriving at, a clearance turns a striking path onto the floor over five ticks without ever adding enough speed to eject the craft, the dive is simulated to its closest approach, the freeze hands it to a closed-form phase clock, the settle spends the speed it earned over 1.2s, and a release leaves exactly along the tangent. **The acceptance gate passes**: at peak boost the craft reaches at worst **61% of a revolution** of release headings and never a full one, over 88 geometries and again over a wider net of 362; §11's second criterion lands at **p50 46.5%** against a band of 35 – 55%. Every tolerance in spec 01 §2 – §12 is an automated test read from outside the simulation — 183 tests, and the headless sweep harness §11 asked for is `test/sim/swing.ts`. Three things are recorded for the author rather than decided: **§11's third criterion holds for every swing that froze on the floor and cannot hold for shallow ones** (a fixed 1.675s stretch against a period that grows as r^1.5); the stand-in corpus **over-represents flybys 72% against a measured 52%** and its percentiles say so; and **grab range scaling with mass is ruled but its shape is a derivation**. `CONTEXT.md` gained **lead**, **orbit**, **boost** and **quality**. **M1.6 done, pulled ahead of M1.4 and M1.5** because the milestone ends in the author flying the build and the first playable moment was three steps away: touch, mouse and keyboard all bound to the one verb, a Canvas2D renderer of circles and lines in the 1170×2532 design space, a hand-authored fixture field carrying the prototype's own geometry, and a **camera** in presentation state. **240 tests.** Three things the specs are silent on were decided rather than invented and are recorded with their expiry dates: the camera is **centred on the craft, fixed sideways and does not lag** — the sideways decision expires when the field outgrows the design space, and the lag is refused because presentation state has no memory and the prototype measured the lag as the thing that costs; the fixture field spreads its radii **34 – 56** so that spec 01 §13.2's exponent is flyable (2.7× reach between the largest body and the smallest at `n = 2`, 1× at `n = 0`); and a press is held while any device holds it. The layer criterion is now written in the direction `pnpm portable` cannot look — **the renderer draws presentation state and asks the simulation nothing** — and spec 00 §1's colour lint exists while the renderer still draws three colours. **One measurement the gate needs before it starts**: the design space is authored at the size of a whole phone screen and a browser gives a page less, so on ADR-0011's measured 393×651 viewport the game draws at **77%** of the size the prototype draws at on the same phone, with a 46-point bar down each side — spec 00 §7 applied exactly as written, and a difference the back-to-back comparison should either discount knowingly or rule away. **The gate has moved** (author, 2026-08-27): it is now flown **after M1.5**, with a recorder running, because M1.5 was always described here as the instrument the gate uses and was still scheduled after the gate that uses it — a session flown without one spends the scarcest input this project has on a sentence nobody can reproduce. The order in M1 is therefore **M1.1 → M1.2 → M1.3 → M1.6 → M1.5 → gate → M1.4**, and M1.5's scope grows to match: the recipe and `pnpm replay`, plus a recorder in the shell, the dev-only phone-to-laptop endpoint extended (**its validator, not its narrowing**), and a reader an agent can walk. **M1.4 is then back in front of the gate** (author, 2026-08-27, after flying M1.6): *"planets are obstacles — I should crash and die."* That is spec 01 §10, already written and not yet built, and 83% of real endings are out of bounds — a field you can pass straight through is not the field spec 01 was measured in. The order is therefore **M1.1 → M1.2 → M1.3 → M1.6 → M1.4 → M1.5 → gate**. The demo also confirmed the camera prediction M1.6 recorded, and both halves of the fix — easing the camera's subject onto the body through a *settled* orbit, and a vertical deadzone — need presentation state to carry memory between ticks, **which is an ADR and is not really about the camera**: spec 02 §5's kick, spec 00 §3's E3 decay and spec 05 §3's wake all decay too. One design question is open and is the author's: **how far a drifting craft may turn off the exit tangent**, given that `CONTEXT.md` and spec 00 §6 fix the nose on it because *"the nose says where; the hand says when"*. All five items are routed under [M1 · Flown](./m1-the-swing.md#flown-2026-08-27--what-the-demo-said). **The camera is now fixed** on [ADR-0015](../adr/0015-presentation-state-carries-what-decays.md) — presentation state carries what decays, and `derive(previous, sim)` is a recurrence evaluated once per tick, which the release kick, the E3 decay and the rungs' wake all needed anyway. Three mechanisms: a lock that makes the *body* the view's subject through a **settled** orbit and is exactly zero through the dive and the oval, a deadzone of **168** design units derived as the median body's floor radius, and an ease at **8**/s bounded from below by the thumb line. Measured over four swings: the orbit swing reaching the view falls from **100% to 0.1%**, the camera never out-runs the craft, and the craft's lowest point is **182** design units below centre against a budget of 422. **M1.4 done** — death, and the shape of a run. Four endings and one contact rule: contact while coasting kills at `R + 5` unless it is a near-parallel graze, contact while a body is held never kills at all, the corridor's line ends a run sideways, the fell-behind line trails the climb by 700 and **does not follow a craft that is holding a body**, and clearing the last body's grab range is the win. The three contacts are **one operation at three sets of constants**, so `dive.ts`'s floor stopped being a second copy of it. `Field` gained a **corridor**, `SimState` gained an **ending** and a **high-water mark**, and both are in the snapshot — a run that ends is a run a recipe reproduces, including how. **280 tests.** **The one decision was where the corridor's line goes, and the obvious answer was measured and rejected**: at the design space's own edges (half-width 195 prototype units) three of the fixture field's twenty-four bodies cannot be orbited at all, because the widest settled circle at a floor reaches **202** — a craft killed on the far side of a swing around a body the field itself placed, which is the exact defect spec 01 §10 records the fell-behind line having had. It is the prototype's own **1.9 × the design width** instead, which is a tuned number with a recorded reason (*"the corridor felt constrictive"*) and the width all 24 of spec 01 §10's recorded endings were flown at; it expires with M3's corridor. **That retires the camera's sideways decision**, exactly as `camera.ts`'s own header said it would: the corridor's line sits **538 design units outside the picture**, and measured over 400 pilot runs **85.5%** leave the frame sideways, **87%** of out-of-bounds endings happen with a body still held, and they land a median of **541** units past the edge — *you are not flying out of the corridor, you are being swung out of it*. Leaving the picture is not new (the fixture field has done it since M1.6); dying out there is. **It is measured and handed over rather than fixed**: the horizontal camera and spec 07's drawn line are both [M3.1](./m3-the-field.md)'s, and it is the first thing that step owes. The headless **pilot** in `test/sim/run.ts` flies whole runs from the two distributions spec 01 measured, and puts out-of-bounds at **71.8%** against a tolerance of 60% and a measurement of 83%; like `swing.ts`'s corpus it is a stand-in and names its two invented numbers, and spec 01 §13.7 replaces it with M1.5's recorded play. `CONTEXT.md` gained **run**, **ending**, **contact**, **corridor**, **line** and **fell-behind line**. Nothing about death is drawn: spec 07 §6's unravelling craft and debrief card are M3's and M6's, and the layer criterion is that deriving from an ended run and an alive one gives the same picture. **And building a demo of the graze rule corrected a reason**: `R + 5` is the craft's own hull — the dart is five prototype units half-width, that number exactly — so a graze is the hull brushing the surface rather than a near miss, and because any line that would put the craft's *centre* inside the disc arrives at more than 0.18, **the graze exemption can never be used to fly through a planet** (it holds for any body under 301 prototype units of radius against a field whose largest is 56). The bounce is there because a hull left alone sinks into the disc it is touching, and it costs the craft **up to 17°** of heading at the lethal threshold, falling to nothing as the pass becomes exactly tangential. Behaviour unchanged; the argument under it was wrong and is fixed in the code, the tests and the plan. **Then the bars became world** (author, 2026-08-28): the design space is fitted whole and the leftover was painted black, which on the author's phone threw away **179 design units either side** that the device could always draw — so the stretch a craft is alive but invisible in falls from **538 to 359** design units, for no change to anything composed. The design space is still the whole contract and nothing the player reads may live in the **bleed**; it is bounded by the corridor's own line, which put a `CorridorView` on presentation state that spec 07 draws its line and bands from in M3. The cost is stated: a desktop window now sees more world than a phone. It does not replace M3.1's camera, and M3.1 now carries the pointer to what that camera owes. Spec 00 §7, `VISION.md` and M3.1 all said *"nothing lands on a letterbox bar"*; the rule survives verbatim — **nothing the player reads is drawn outside the design space** — and all three are rebased on it (author, 2026-08-28). **M1.5 done** — the recipe, the replay and the trail a session leaves, which is the instrument the gate is flown with. A run is written down as its **seed, its input log and the field it was flown in**: the log is the ticks the press changed on, recorded from the same boolean handed to `stepSim` and before it is handed over, so the reconstruction is identical by construction — including the up-edge nobody made, because **focus loss lets go of everything** and a log written from the events would not have recorded it. `pnpm replay` flies one and prints the trail; the loop itself is `src/sim/replay.ts`, pure and portable, because that is the seam a verifying service runs through (ADR-0003). **The acceptance has two readings and both are checked**: four independent replays compared at every tick rather than only at the end, and each carried to four times the run's own length — the second only bites on a run still flying when its log ran out, because a run that has ended does not tick. **364 tests.** The field is named rather than carried, borrowing spec [17 · §2](../spec/17-daily-field.md)'s versioning early, and the version is held to the field by a fingerprint test — which covers **`MASS_EXPONENT`**, the one parameter the gate is about to decide, so moving it fails loudly instead of replaying every old recipe against a field nobody flew. What that does not cover is recorded rather than papered over: a constant that shapes the *swing* moves the run without moving the field's identity, and a simulation behaviour version is the next thing this needs, beside `field` in `Recipe`. **The endpoint's 512KB cap wanted a measurement and got one**: at spec 01 §3's recorded press rate an 85-second run is **50 presses, 100 edges and 564 bytes**, the whole 474-second cohort is 3.2KB, an hour of unbroken play is 27KB, and the timing reports on disk are 1.7 – 1.9KB — so the cap is now **64KB**, eight times narrower. Everything else about it narrowed too: a second validator rather than a relaxed one, a filename suffix that is a literal per kind so a caller cannot choose part of a path, and a dispatch written to disk as the object the validator built rather than the bytes that arrived. `CONTEXT.md` gained **dispatch** — a recipe, the ticks the author flagged while flying, and what they wrote afterwards; the evidence and the testimony in one envelope, and explicitly not a second name for a recipe. **The trail reads from outside**: which tick a grab happened on, the address, the geometry the press saw, the freeze, the depth, and where on the boost envelope the release fell — and, for any tick, **where the camera was looking**, because ADR-0006's promise that a frame is a pure function of `(recipe, tick)` is the reason the whole step exists and [ADR-0015](../adr/0015-presentation-state-carries-what-decays.md) means it only holds if the picture is derived beside the run from tick zero. A test proves the distinction bites: asking the final state what it looks like gives a different answer than arriving there. **Flown at the bench, 2026-08-28** — the gate opened on a desktop bench with the open questions on sliders, and moved two things. **The clearance's rate is now the characteristic and its duration the consequence**: 47% of grabs owe one, the median owes 59.5° of turn, and five fixed ticks paid that at 11.9°/tick against a settled orbit's own p90 of 5.07 — three and a half times faster than the thing it hands the craft to, which is the corner that reads as a snap. It now eases at the orbit's rate over five to ten ticks; the p90 biggest single-tick turn falls **12.5° → 6.9°** and the count of grabs containing a tick over 8° falls **400 → 57**, with no periapsis below the floor at any swept duration. Longer is not better and ten is measured: the clearance re-asks each tick what it owes and a turn buys less momentum the closer the craft gets, so past twelve ticks §5a's speed band starts failing. **The neighbour bounce came down 0.6 → 0.2**: over 300 runs the old value flipped the craft more than 90° in one tick sixteen times, up to 165°, and below 0.2 it stops bouncing and starts skidding (86 ticks of contact at 0). **The camera follows at 3**, the author's own value, which costs two design units of movement as the lock arrives against the 49 that was reported as a defect. **A recipe now names the swing it was flown under** — the gap M1.5 recorded, landed by the first change that needed it, with a fingerprint test over three whole runs keeping the number honest. What was measured and deliberately **not** moved is the corner at the **freeze**, where the turn rate steps a median ×3.6 in one tick: that is §6a, which is load-bearing. **369 tests.** **And the bench is in the repo now**, as `pnpm bench`: the game bundled for the browser with the open questions on sliders, built from a patched copy of `src/` so the constants stay `const` in the game itself, typechecked against the copy it imports, and held by `test/bench.test.ts` — which fails the moment one of those constants is renamed or moved, because a bench whose sliders are wired to nothing answers questions confidently and wrongly. **385 tests.** **Flown, and what came back is a sentence and five rulings** (author, 2026-08-28). The author flew the build, moved the five things above, and said *“this feels alright”* — then went on to planning M2 and M3, **explicitly deferring** the two numbers the gate was carrying for it: the mass-to-radius exponent (spec 01 §13.2) and the eccentricity cap (§13.5), *“I'll want to fine-tune things after full rewrite.”* **That is not the sign-off this milestone's acceptance asks for, and it is recorded as what it is rather than upgraded.** The gate is written as *“the author flies this build and the prototype back to back and says yes or no”*, and **no back-to-back is recorded** — so the gate is **open**. **M2 proceeds anyway** (author, 2026-08-28, asked plainly), because the author moved on to it; what M2 must not do is read five rulings and a sentence as a verdict on the swing, and the two deferrals stay the author's to close after the rewrite rather than anything's to pick up. |
| [M2](./m2-the-instrument.md) | The instrument | The compass, the release, the 400ms | **author** | **M2.1 done** — the presentation-state layer for real, which in practice means **the decay machinery the design needs everywhere, built once**. [ADR-0015](../adr/0015-presentation-state-carries-what-decays.md) exists because spec 02 §5's camera kick, spec 02 §4's stretch, spec 00 §3's E3, spec 05 §3's wake and spec 06's callouts are one idea in five costumes, and `src/state/decay.ts` is now that idea: a decay that **ends rather than becoming small** (so spec 00 §5's *“nothing persists past 600ms”* is something a `null` satisfies and a float cannot), a fall to nothing, a return home past rest, and the exponential ease the camera had been keeping to itself. **Two curves and both are argued.** The fall is the square of what is left, because *“400ms exponential”* cannot be taken literally — an exponential never reaches zero and the persistence rule in the same table demands an end — so what is kept is the behaviour and the curve is marked an opening position, on the bench. The return crosses rest at **0.37**, which is not a taste: it is read backwards from the one overshoot the design puts a number on, spec 02 §4's **0.95 against a 1.5 stretch**, and the closed form `4(1 − c)³ / 27c` puts a tenth exactly there. **The ordinal channel is spec 00 §3 read into design units, and the reading is the finding.** The boards frame the game at phone size — Direction 01's artboard is 430 × 760 — and the design space is a phone at three device pixels to the point, so 6 / 18 / 48px are **18 / 54 / 144** design units: the same ×3 the author confirmed on 2026-08-27, arriving from the other side. It is stated rather than assumed because the alternative is not close — taken as raw design units, E2's 18 would be a glow narrower than the 45-unit craft it haloes. **One E3 alive at a time is now a shape rather than a check**: it is one nullable field on the whole presentation, so a second one has nowhere to be. That is worth more than it looks, because the release, the grab, the award and the checkered line all want an E3 and are built in four different milestones. **`SIM_VERSION` did not move and could not have**: a grab is a body being held that was not, read off the previous *picture*, so nothing was added to the simulation for the picture's benefit. **The craft's stretch is built at spec 02 §4's amplitudes, dated from `T0`** rather than `T+70ms` — which is not a decision but the rebase [ADR-0012](../adr/0012-the-punch-is-bought-with-speed-not-with-stopped-time.md)'s own notice already prescribes, and M2.4 still owns the file. Two costs are stated: the across axis rebounds to **1.03** against the board's 1.06, and the rebound is deepest at **58%** of the return against the board's 83% — four hand-set keyframes are not a curve, and the depth is the half worth matching. **Three things in M2.1's own list were deliberately not built.** The **camera kick** is spec 02 §5's and travels *along the exit tangent*, which has a horizontal component, and `camera.ts` is vertical-only until [M3.1](./m3-the-field.md) with a test asserting `camera.x` never moves — so it goes where spec 02 is rewritten, in M2.4, rather than being half-built here and rebased there. **Live awards** need a window to be graded inside, which is M2.3's. And the **chain** is a named zero: it needs rungs (M3) and an economy (M4), so the term it multiplies is built and only its value is missing. **The renderer learned the channel** — a bloom it is told the radius of, an additive E3, and the stretch applied after the rotate so it is along the velocity and not a screen axis; bodies keep their crude rim until M2.2 draws them properly. Interpolation gained spec 00 §5's first rule as code: **things arrive, they do not fade in**, so a decay struck on the later tick is taken whole rather than eased up from nothing across 16ms. **The bench grew a third card and eight knobs** — energies, the E3's length, the stretch and the overshoot — and the three cards are ADR-0006's wall made into something a mouse can feel: physics restarts the run, the picture and light land live. **431 tests, 39 files.** The acceptance is asserted at named ticks of the run `pnpm replay` ships, with no canvas in the file: the grab lands on **74** and the release on **258**, the E3 is at full radius on the tick it is struck and gone on **282**, and the stretch is home on **269**. `CONTEXT.md` gained **bloom**, **flash**, **decay** and **deformation** — decay with an explicit line saying it is never the **settle**, which the glossary already forbade that word for. **M2.2 done** — body language, and the sightings that are the other half of spec 00 §7's ruling. **A hue is a name, so it is generated a layer below the paint**: spec 00 §2 fixes the lightness and the chroma *“so that every identity is equally loud”*, which leaves one free coordinate, and that one is a number on presentation state derived from the address — because *“two live targets too close in hue to tell apart”* is spec 04 §5's one reason to print an address, and no test could reach that fact if the hue were chosen inside a `fillStyle`. **The step is the golden section of what the reserved ranges leave**, which is the step's one real decision: three arcs totalling **167.3°** have to hold a day's forty bodies at ≥50° between neighbours, and the golden step gives **63.9°** between neighbours and 40 distinct hues. A step tuned to this arc beats it — 106.9° — by sitting a hair off two fifths of it, which collapses to **six** repeating hues the moment §2a's colour-vision sweep changes the arc's length, and that sweep is flagged to change exactly that. **One reading is recorded rather than assumed**: §2's four windows read strictly leave 315.5 – 337.7 open, a magenta shoulder on ION's own window, and it is shut on the milestone brief's own phrase *“the violet–pink band”* with the argument beside it — while flying it found the other end of the same question, an identity just above 17.7 reading as a soft salmon, handed to §2a rather than fixed. **The tide's three numbers are one number**: §2 requires longer, brighter and tighter-tracking to *“move together and monotonically with mass”*, which three separate formulas break silently, so all three read `m / (m + median)` — strictly monotone, bounded, and exactly a half at the median body, so §2's reference values fall out rather than being written twice. The saturation is not a flourish: an arc lives on a circle, and a law with no ceiling eventually draws a tide that is a ring. **SPENT is the first memory in this layer that never converges**, and it is named rather than smuggled — ADR-0015's third rule does not reach it, because an eased value is a feedback loop that can drift and a latched event flag is never an input to its own next value; it can only be wrong from the start, which is the recipe's own exposure. **That is a real widening of what ADR-0015 contemplates and the ADR does not say so yet**, so it is flagged for the author rather than written into it. **Sightings are pinned to the design space**: *“off the picture”* has to mean outside the *design space* rather than outside the buffer, because presentation state must not know a viewport — the count of marks would stop being a function of `(recipe, tick)`. So they carry **design-space** coordinates, alone among the positions in this layer, and the cost is stated: a wide window can show a body in the bleed and a mark for it. Three rules pick them, two of them §6's and the third derived — **a spent body has none**, because §6 draws the mark in the identity hue and spec 04 §3 says a spent body's lamp is out. **The palette lint got stronger on the way past**: identity is the one colour in the game that is built rather than named, so it arrives as a template, and the lint had only ever scanned plain strings — any render file could have assembled a colour and passed. **476 tests, 43 files**, with spec 04's acceptance asserted without a canvas and §1's *“radius 20 and radius 200 produce identical stroke widths”* asserted against a context that records what it was asked to draw. `CONTEXT.md` gained **strata** and **spent**. **M2.3 done** — the compass, and it is **a fact the world can be asked for** rather than a hint drawn over it. Two of the game's own rules make the question answerable: a coasting craft feels nothing from anything at any distance, so a release is a **straight ray** and where it goes is a line-and-circle problem; and the nose points along the exit tangent for the whole orbit, so the release *angle is the aim*. That is why the instrument can be drawn on the orbit path — the path and the aim are the same coordinate, which is spec 01 §11's tension in one sentence. **A window is where you actually end up**: every release angle is answered with one body, the first whose grab range the ray enters, so the windows partition the orbit instead of overlapping it, a body hiding behind a nearer one gets none, and **the width is earned by the geometry** rather than assigned — which makes spec 00 §6's *“the arc's width is the posted odds”* true instead of asserted. **Two readings were built and rejected loudly**: the unbounded ray drew **23 rings**, because the median body is on offer from 1 680 units and a line up a corridor eventually passes near everything in it; and *first range **entered*** drew **one 360° window**, because a craft on its orbit is routinely already inside a neighbour's range. Asking where the ray gets **nearest** instead makes flying away from a body you are standing beside a release that does not arrive at it. **Neither bound is invented.** The ray is traced exactly as far as the craft could survive — spec 01 §10's own corridor, foot and fell-behind line — and the **ring count is measured**: over 120 pilot runs and 342 releases that converted, the body actually grabbed next is among the **four** nearest to the one just left **100%** of the time (99.7% at three, 92.7% at two, 68.1% at one), which is close to what Direction 01's board draws. Rings are ordered **nearest first and not best-aimed**, because a stack sorted by aim reshuffles its own radii every tick. **The rings breathe**: the innermost is the craft's own radius, so the trail lies on the path actually flown and the stack swings with the oval through the settle. **Two things the specs say could not be built and are recorded rather than invented**: spec 00 §6's per-ring **label** collides with spec 04 §5's rule that an address chip appears in exactly one case, and with `P11` retired a label that is not an address has nothing to say — one of the two is stale and the specs do not say which; and §6's **ghost** is `CONTEXT.md`'s word for a replay flown beside a live run, so the mark where the hand cuts a ring is the **crossing**, which is what this milestone's own brief already called it. **Flying it found two things past the picture**: spec 06's 1.5° PERFECT floor is absolute while every other zone is a fraction, so the narrower the window the larger a share of it pays the top word — **42%** at the fixture field's p10 width of 7.2° against **16%** at 40°, and all of a window under 3° — which is §6's *“automatically a better-paid one”* as a mechanism, worth seeing before M4 prices it; and graded against the pilot's own 351 releases the compass calls **77% of them misses**, which is the M1 gate's finding from the other side, since the pilot has no compass either. **509 tests, 46 files**, and the load-bearing one runs the instrument through `stepSim` — hold to the dot, let go, coast, and the craft **arrives within that body's grab range**, with nothing read back from the compass's own arithmetic. `SIM_VERSION` did not move: the compass is asked *of* a state and never writes to one. `CONTEXT.md` gained **crossing**. **Flown, 2026-08-29** — six notes, and **two of them were the prototype telling this repo it had already solved the problem and been ignored**. The compass bounced because M2.3 anchored the rings to the craft's *live* radius; the prototype's own compass carries the measurement of exactly that — *“85 out to 97 and back over about a second, on top of a curve the player is trying to read”* — and its fix, the periapsis, is the one now taken. And every body glowed because M2.2 read spec 04 §3's *“E0–E1”* at the top of its range and lit twenty-four bodies at once, where the prototype gates it on live pull and says so outright. That gave the layer the quantity it was missing — **grip**, the live pull against the pull at a body's own floor — which gates the bloom and draws the wide faint halo the author asked for. **The window was measuring the wrong thing**: at grab range it is p50 **360°**, which is true and useless, because the median body is on offer from 1 680 design units against a field spaced nearer 700. Measured against the **floor** instead — the one guarantee a grab makes — it is p10 18°, p50 24°, p90 40°, and spec 06 §2's worked examples (15° and 40°) and the prototype's fixed 40° wedge agree from two more directions; built, it lands p50 **18.6°** and never above 20.4°. Two bounds came with it and neither is invented: **`AIM_RANGE`**, the prototype's two body-spacings, which is what spec 00 §6's *“reachable”* always lacked and which bounds the **partition** rather than only the drawing — bounding only the drawing left far bodies still *winning* release angles nobody would fly to, squeezing half the arcs below the floor — and a **minimum width**, ruled, because *“for very distant planets we still need to show a window”*. **The compass grades the width it draws**, on the prototype's rule that *“the player must never be scored against something they cannot see.”* **The rings say how far**: a fixed clearance then an offset proportional to distance, so the innermost is the next hop and reading the stack is reading the field. **The path is the oval and it rounds out** — it was an arc of a *circle* at the ring anchor, which is not the line being flown; it is now the simulation's own ellipse sampled at the shape it has this tick, so spec 01 §6's settle is drawn for the first time. **Three things were deleted**: the E3 at the grab and the release (*“let the PLANET speak about our grab”* — spec 04 §3 already had it doing that, and the release goes quiet until M2.4), spec 04 §2's **inner ripple** (a board line the prototype never implemented), and the tide on every body in reach (it is on the body a press would take, which needed `offered` — spec 03 §6's own deferred cue, revisited now that the compass exists). **And sightings point**: the 2026-08-28 ruling that a sighting does not point is reversed — *“this is another instance of an original rule being too strict”* — so they are arrows in the identity hue with a distance beneath, fading with range and full strength on the offer. The label is a **distance and not a name**, which is a reading and is recorded as one: the `P11` retirement is about naming, and identity stays hue-only. **525 tests, 46 files**, every moved number on the bench, and the eight spec sentences the build has made false are **listed for approval rather than edited** — `docs/` is the author's. **Flown again, 2026-08-29** — four more notes about the instrument, and answering them **deleted** the partition machinery M2.3 built: the model is the prototype's now and about half the code. **Windows blinked because the ring set was derived from the geometry**, so it shifted as the orbit rounded — *“once they're on the compass they should stay.”* Targets come from the **field** now (above the held body, within `AIM_RANGE`, nearest first), which cannot change while a body is held, and it brings the prototype's **upward-only** rule and its reason: offering the planet you just left *“invites you to bounce between two bodies forever.”* **The dot is solved rather than sampled** — it was the minimum of the miss *within the arc a body won*, so it sat at an edge whenever the true best release lay elsewhere; it is now the root of the signed heading error, exact to a thousandth of a degree and cheaper than what it replaced. **The window is hand-wavy on purpose**, at two floors of reach: p50 **18.6° → 36.6°**, which is Direction 01's own 40° wedge. **Occlusion stopped deleting windows** — a blocked run is dimmed to 30% and kept, because a vanishing window is the blinking again. **Stacked windows push their rings apart**, the ring and not the arc, which is spec 00 §6's own instinct for labels one element up. **And the glow arrives before the hand**: the heat ramped over the window's own width, so a window was dark until the hand was inside it — measured, **3 to 4 ticks, 50–67ms**. It ramps over a quarter turn now, the prototype's `alignment`, and a window is lit **15 ticks, 250ms** before the dot. **One thing the notes imply and this cannot fix**: far windows are narrower, spec 00 §6 makes a narrow window *“automatically a better-paid one”*, so the hardest window is worth the most and the far one is hardest — guiding play toward near bodies means pricing distance, which is spec 08's arithmetic and **M4's**. **Flown a third time, 2026-08-29** — four notes, and one of them was not a bug. **The oval fades in from the press**: `predictOrbit` is the osculating ellipse of the live state, drawn from the moment gravity binds the craft and converging on the frozen orbit, so the freeze is not a moment the path jumps at — the prototype found the same hole from the other side, *“2.0 seconds of blank sky from the grab, which is precisely when a player is deciding where this capture is taking them.”* The fade is **not** spec 00 §5 being broken: *“things arrive, they do not fade in”* governs elements entering, and what fades here is a prediction firming up. **The compass shows bodies below again and the climb moved into the press** — filtering the instrument was the wrong half of the idea, so the picture shows everything reachable and the *grab* leans upward, which is a preference the player can overrule by flying at something. It had to be smooth: spec 01 §3 calls a threshold *“a cliff the player falls off”*, so `CLIMB_BIAS` weights how far above or below, saturating, in the body's own grab range. Swept over 200 pilot runs, downward grabs fall 15.3% → 12.8% at 0.5 — **weak evidence and it says so**, because the pilot presses at sampled distances rather than choosing a target. **`SIM_VERSION` is 3**, and the shipped run is re-recorded at it. **The tide grows into its width and it is an A/B**: `TIDE_GROWTH` mixes spec 04 §2's mass-only width at 0 with the prototype's proximity reading at 1, nothing deleted at either end, and a half-width of 6.3° at grip 0.03 opening to 18° — the waterdrop falls out of a round-capped arc a few degrees wide. **And the last grab was not a bug**: over the 302 ticks that swing spent settled, `v / circular = 1.000000` at every one and the radius held to 0.01. The 43% loss is the freeze clamp (1 181 → 837, spec 01 §6a's own mechanism), the settle (837 → 604) and a **373-tick hold against an envelope gone by 156**. **What is missing is a clock**: §11's tension is the envelope in time against the window in angle, M2 has drawn the window beautifully and drawn nothing for the envelope, and the prototype says it in words. It is a new readable element and therefore spec 03's and spec 00 §4's, so it is **the first thing M2.4 should be asked about** — until it exists the gate is judging half of §11. **And a fourth sitting.** *“Where is the A/B slider?”* — it was never on the dev server, and that was the honest answer to the wrong question: the bench is a **separate build** from a patched copy of `src/`, because the constants stay `const` in the game (AGENTS.md §6). The build is unchanged; [`vite-plugin-bench.ts`](../../tools/vite-plugin-bench.ts) serves it at **`/bench`** in dev, so the QR that puts the game on a phone puts the knobs on the same phone — and it refuses rather than serving a page whose sliders are wired to an older copy of the simulation. **A planet's bloom takes a third of the table's strength**: a held body jumping to E2 on top of its grip halo read as a lit blob rather than a rim, and it is a *body's* scale and not the table's because Direction 01 rules the craft the brightest thing on screen always. **And the compass comes online with a pop** — the bounce being remembered was accidental, the prototype's ring radius following the ship all swing, which its own comment measured at *“85 out to 97 and back over about a second”* and removed; what is built is spec 00 §5's **ENTER** token, 120ms from 92% with one overshoot, fired once at the freeze, scaling the instrument and never the path. **And the windows were moving, which was three bugs wearing one coat** — *“the planets don't move. We should only show stable targets.”* The **dot was chosen from two exact answers by floating-point noise**: a circle has one release whose tangent points at a body, an ellipse can have two, and through the settle every orbit is one — measured, the dot flipped **46.6° in a single tick, twice**. The tie-break is the shortest flight; two roots are rare (7 target-ticks in 3 503) and the coarse sweep was exonerated (0 misses in 3 503). Then the **dot was computed on the orbit the craft was momentarily on**, so it still slid as the settle rounded the oval — p90 **36°**, up to **56°**, a target moving out from under the aim closing on it. The instrument is now anchored to the orbit the swing is **becoming**, which is the anchor the rings already used, and the path still draws the one the craft is on. **What that costs is measured**: a fixed dot's aim misses p50 6.6° through the *unarmed* stretch where a release is paid nothing, **p50 0.62°** across the boost's plateau, and exactly zero once settled — against a window half-width of 18°. Largest where nothing is at stake, gone where everything is. **The third bug went with the second**: the ring radius jumped a whole `STACK_GAP` whenever sliding dots stopped overlapping. Every ring's dot, width and radius is now **exactly** constant for a whole swing — 0.00 at every percentile, asserted as equality rather than a tolerance. **And the instrument clicks out** rather than vanishing at a release: it **leaves on the curve it arrives on, reversed**, so the swell is the same single overshoot ENTER lands on and the two ends are one shape. Measured, it swells to **1.034** by five ticks, back through 1.0 at seven, collapses to 0.78 and is gone at **eleven — 180ms**. One curve was added and the reason is a measurement: the design's `fade` is fastest at the start, which had the compass at 30% opacity before it *began* to collapse and under 13% through the collapse — the motion asked for, where it could not be seen. `shut` is the same journey with the curvature reversed, so the swell lands at **79 – 87%** and the deepest collapse is still visible at **17%**. The **path** deliberately does not scale with it: spec 02 §6's farewell ring is the same instant going the other way, and that expansion is M2.4's to put on rather than half-invented here. **548 tests, 46 files.** |
| [M3](./m3-the-field.md) | The field | Rungs, sky, anomaly, boundary, retro grade | — | not started |
| [M4](./m4-the-economy.md) | The economy | Carry/cash/bank, tiers, fuel, HUD, ZEN | **author** | not started |
| [M5](./m5-audio.md) | Audio | The swing has a voice | **author** | not started |
| [M6](./m6-the-run.md) | The run | Front door, finish, debrief, results, DRIFT | **author** | not started |
| [M7](./m7-powerups.md) | Powerups | The field's generosity | — | not started |

## What is deliberately not here

- **No backend.** Offline v1, with the seams cut for a service later (ADR-0003).
- **No CORRIDORS, no ADVENTURE.** Both need authored content pipelines nobody has designed
  (ADR-0005).
- **No lives inside DAILY.** Unlimited play lives in DRIFT (ADR-0007).
- **No new body types in v1.** Difficulty comes from geometry first, so that when types
  arrive there is something to measure them against.
